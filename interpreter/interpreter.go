// Package interpreter implements an interpreter for the AST.
package interpreter

import (
	"fmt"
)

type Interpreter struct{}

func New() (*Interpreter, error) {
	return nil, fmt.Errorf("!implemented")
}
